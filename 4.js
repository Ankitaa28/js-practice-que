// write a function to caculate the area of a rectangle given its length and breadth
let length = 15;
let width= 30;
function area(l,b){
    return l*b ;
}
console.log("area of rectangle is"+" " + area(length,width));